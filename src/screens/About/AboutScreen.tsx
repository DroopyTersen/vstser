import * as React from 'react';

export default class AboutScreen extends React.PureComponent<AboutScreenProps, {}> {
    render() {
        return (
            <div>AboutScreen</div>
        );
    }
}

export interface AboutScreenProps {
    //props
}